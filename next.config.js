/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: false,
  // trailingSlash: true,
  // output: 'export',
  experimental: {
    appDir: true,
  }
}

module.exports = nextConfig
