/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: false,
  // trailingSlash: true,
  // experimental: {
  //   appDir: true,
  // },
  // output: 'export'
}

module.exports = nextConfig
